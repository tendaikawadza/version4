import { HideForRolesDirective } from './hide-for-roles.directive';

describe('HideForRolesDirective', () => {
  it('should create an instance', () => {
    const directive = new HideForRolesDirective();
    expect(directive).toBeTruthy();
  });
});
